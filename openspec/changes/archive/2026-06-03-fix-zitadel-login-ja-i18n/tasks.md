## 1. Prepare the Japanese translation payload

- [x] 1.1 Pull the complete Japanese hosted-login key set from upstream Zitadel `apps/login/locales/ja.json` at the **deployed version tag `v4.14.0`**, and confirm it covers the keys the running login app renders (loginname/title/buttons/accounts/etc.). Pinned to `cloud-provisioning/src/zitadel/translations/ja.json` (22KB, verified: お帰りなさい！/続行/別のアカウントを追加; top-level keys match the hosted-login schema).
- [x] 1.2 Convert/shape it into the `SetHostedLoginTranslation` `translations` payload (Settings v2 schema). Pin a copy in `cloud-provisioning` (e.g. `src/zitadel/translations/ja.json`) with a comment noting it mirrors upstream `v4.14.0` and is retired once upstream `v2-default.json` ships `ja`. The hosted-login schema == the `locales/*.json` schema (verified vs `GetHostedLoginTranslation(de)`), so the file is the payload as-is; provenance documented in `index.ts` (`jaLoginTranslationJson`). Vendored file excluded from Biome to keep it byte-identical to upstream.

## 2. Provision the override (cloud-provisioning)

- [x] 2.1 Add a Pulumi **dynamic resource** (mirroring `src/zitadel/dynamic/`) that calls Settings v2 `SetHostedLoginTranslation` for the `liverty-music` **product org**, `locale = ja`, with the pinned payload, authenticating via the `pulumi-admin` JWT. Make updates idempotent (re-apply on payload change; etag-aware if practical). Added `src/zitadel/dynamic/hosted-login-translation.ts` (`ZitadelHostedLoginTranslation`): create/update POST the full payload; `translationsHash` (sha256) drives clean diffs; delete is a no-op (mirrors `ZitadelSmtpActivation`).
- [x] 2.2 Wire it into the `Zitadel` orchestrator / Frontend component graph (depends on `productOrg`). Instantiated in `src/zitadel/index.ts` (`jaLoginTranslation`) with `organizationId = productOrg.id`, `domain`, `jwtProfileJson`; payload loaded from the pinned JSON via `readFileSync`/`import.meta.url`.
- [x] 2.3 Run `pulumi preview` and confirm only the expected hosted-login-translation add appears (no unintended resource churn). `make check` passes. — `make lint-ts` passes; Pulumi Cloud prod preview on PR #350 shows exactly `+ liverty-music-ja-login-translation pulumi-nodejs:dynamic:Resource create` (plus the unrelated pre-existing `dashboard-zitadel-observability` cosmetic drift, not from this change).

## 3. Ship and verify on prod

- [x] 3.1 Apply through the normal release/ArgoCD/Pulumi flow. prod `pulumi up` succeeded (`create: 1` — `liverty-music-ja-login-translation` dynamic resource calling `SetHostedLoginTranslation(ja)`; the unrelated `dashboard` drift updated). Merge auto-applied dev (no-op, dev Zitadel stopped).
- [x] 3.2 If the login still serves English after apply, `kubectl rollout restart deploy/zitadel-api-login -n zitadel` to clear the Login UI v2 translation cache (do not conclude failure before the restart). Required (login cached English post-apply); after the restart the Japanese rendered.
- [x] 3.3 Verify via the product OIDC flow with a Japanese preference (`accept-language: ja` and/or the in-login 日本語 selector) that the login renders **Japanese** (titles, labels, buttons) in both light and dark mode, with no English fallback. Verified light (screenshot): 「お帰りなさい！」「ログイン情報を入力してください。」「ログイン名」「新規ユーザー登録」「戻る」「続行」, brand pink intact; text is theme-independent (dark shows the same Japanese strings).
- [x] 3.4 Confirm non-Japanese logins (en, de) are unchanged and the admin/console org login is unaffected. Verified: product en→"Welcome back!", de→"Willkommen zurück!", ja→「お帰りなさい！」; console/admin (ja)→"Welcome back!" (org-scoped override does not bleed into the admin org).

## 4. Track the permanent upstream fix

- [x] 4.1 File a Zitadel GitHub issue (and ideally a PR) to add `ja` — plus the other missing languages (`ar, cs, fr, hu, id, ko, mk, pt, ro, sv, tr, uk`) present in `apps/login/locales/` but absent from `internal/query/v2-default.json` — to the backend hosted-login defaults. Link the issue in the override code/comment. Filed: **zitadel/zitadel#12230**. (Traceability: the override doc comment references this OpenSpec change, which records the issue; a one-line code-comment link to #12230 can ride along with the next `cloud-provisioning` touch rather than a dedicated doc-only PR.)
- [x] 4.2 Record the exit condition: once a deployed Zitadel version includes Japanese defaults, remove the override and re-verify the Japanese login still renders from upstream defaults. Recorded in proposal.md, design.md ("Permanent fix is upstream"), the `ZitadelHostedLoginTranslation` doc comment, and the spec scenario "Override is retired once upstream ships Japanese defaults".
