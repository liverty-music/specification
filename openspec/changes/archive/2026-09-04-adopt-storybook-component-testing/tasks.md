## 1. Toolchain upgrade & Storybook config minimization

- [x] 1.1 Bump `aurelia`, `@aurelia/router`, `@aurelia/i18n`, `@aurelia/vite-plugin` to `2.0.0-rc.2`; run the full existing unit + smoke + e2e suites as the safety net (Risk R5)
- [x] 1.2 Upgrade `@aurelia/storybook` `2.2.1 → 3.0.0` and `storybook` + `@storybook/builder-vite` `10.1 → 10.6`
- [x] 1.3 Remove the hand-written `viteFinal` (`@aurelia/runtime-html` optimizeDeps exclusion) from `.storybook/main.ts`; delete `.storybook/preview.ts`; drop `@storybook/addon-links` if unused (D5)
- [x] 1.4 Delete `src/routes/welcome/welcome-route.stories.ts` (route out of scope; spec: "Route components are excluded")
- [x] 1.5 Verify `npm run storybook` (storybook dev) boots with no component stories yet

## 2. Vitest 4 migration & projects topology

- [x] 2.1 Upgrade `vitest` and `@vitest/coverage-v8` `2.1.8 → 4.x`; review Vitest 4 breaking changes against the current unit suite (Risk R1) — pinned to `4.1.11` (addon-vitest peers `^3||^4`, not v5). Breaking-change fixes: constructor `mockImplementation` must be a function expression (not arrow) under v4; `test/setup.ts` now installs a Web Storage polyfill + `matchMedia` stub (v4 no longer forwards `--no-experimental-webstorage` to workers); coverage `all:false`; unmocked routes in `app-shell.spec` mocked to stop a teardown-race unhandled error
- [x] 2.2 Restructure `vitest.config.ts` into `test.projects` with a `unit` project carrying the current jsdom config verbatim (forks `--no-experimental-webstorage`, excludes `e2e/**`/`scripts/**`, v8 coverage thresholds statements/functions/lines 70 & branches 78) — branch threshold recalibrated 78 → 60 (Vitest 4 `ast-v8` branch remapping is a measurement-basis change, user-approved; see design note)
- [x] 2.3 Resolve OQ1: fold `vitest.scripts.config.ts` into the projects layout as a third project, or keep it standalone — decide by whether the Node-API script tests compose cleanly — FOLDED IN: `scripts` is now a third bare (no-plugins) project; `vitest.scripts.config.ts` deleted, `make test`/`test`/`test:scripts` updated
- [x] 2.4 Run `make test`; confirm the `unit` project is green and coverage is enforced at the preserved thresholds (spec: "Unit coverage thresholds are preserved") — `make test` green (161 files / 1728 passed), coverage enforced (statements 80 / branches 61 / functions 75 / lines 81 against 70/60/70/70)

## 3. Component-test project & PoC gate

- [x] 3.1 Add dev deps: `@storybook/addon-vitest`, `@vitest/browser`, `@vitest/browser-playwright`, `@storybook/addon-a11y` (+ `@storybook/addon-docs` for autodocs), all `10.6.0`/`4.1.11`
- [x] 3.2 Add the `storybook` project to `vitest.config.ts`: `storybookTest({ configDir: '.storybook' })`, `browser: { enabled, headless, provider: playwright({}), instances: [{ browser: 'chromium' }] }`, `setupFiles: ['./.storybook/vitest.setup.ts']`; create `.storybook/vitest.setup.ts` (uses `setProjectAnnotations` — REQUIRED for the Aurelia render function, else SB_PREVIEW_API_0014)
- [x] 3.3 Verify the `storybook` project does NOT pull the app `vite.config.ts` build-only concerns (multi-entry `input`, VitePWA) into the browser build (Risk R2 / D6) — resolved by NOT `extends`-ing the app config (its `nodePolyfills` also breaks addon-vitest's Node setup); the project adds ONLY the Aurelia plugin + `storybookTest` + a `plain-date-engine` alias + `optimizeDeps.include` for the addon entries
- [x] 3.4 Register `addon-a11y` so accessibility checks run inside the browser project and fail on violations (spec: "Accessibility checks run on component stories" / D3) — VERIFIED enforcing: `.storybook/vitest.setup.ts` composes `@storybook/addon-a11y/preview` + a plain `{ parameters: { a11y: { test: 'error' } } }` annotation (params inside `definePreview` do NOT propagate through `setProjectAnnotations`; shared config extracted to `.storybook/story-annotations.ts`). A deliberate color-contrast violation fails the run; the real stories pass axe
- [x] 3.5 PoC: author one story (e.g. `error-banner`) and run it via `vitest --project=storybook`; confirm `@storybook/addon-vitest` executes an Aurelia v3 story in browser mode. If it fails, fall back to `composeStories` portable stories (Risk R3 gate — do not fan out until green) — GREEN: used `loading-spinner` (pure `@bindable`, no DI) — 3 stories pass incl. a `play` interaction assertion + a11y; no `composeStories` fallback needed
- [x] 3.6 Add `test-storybook` script (`vitest --project=storybook`) to `package.json`

## 4. Author component stories

- [x] 4.1 Write CSF3 stories colocated under `src/components/<name>/` for the presentational set: `loading-spinner`, `toast`, `snack-bar`, `error-banner`, `inline-error`, `state-placeholder`, `page-header`, `coach-mark`, `svg-icon`, `bottom-sheet` (D2) — all 10 authored; DI/child-bound ones use `defineAureliaStory` + `parameters.aurelia`/`items` (svg-icon + bottom-sheet globally registered in `preview.ts`; i18n configured there; snack-bar via an EA-publishing host; error-banner via an `IErrorBoundaryService`/`I18N` mock)
- [x] 4.2 For each, expose `@bindable` inputs as controls and enumerate visually distinct states as named stories (spec: "Distinct states are enumerated as stories")
- [x] 4.3 Add `play` functions with interaction assertions where components have behavior (e.g. bottom-sheet open/close, toast dismiss) (spec: "Interaction assertions are verified") — inline-error retry, bottom-sheet tap-outside-dismiss (`sheet-closed`), coach-mark activate, snack-bar publish, error-banner Dismiss→service spy
- [x] 4.4 Tag stories with `test` (and `autodocs`) so they execute under Vitest and generate docs
- [x] 4.5 Confirm route/canvas/RPC/auth-bound components remain unstoried (spec: "Route components are excluded") — only the 10 presentational elements are storied; `dna-orb`, routes, and RPC/auth-bound components have no stories

## 5. Component-level visual regression (Vitest toMatchScreenshot)

- [x] 5.1 Add `toMatchScreenshot()` assertions for the design-system subset within the `storybook` browser project; set an initial pixel tolerance (OQ2) — subset = `svg-icon`, `inline-error`, `state-placeholder`, `page-header` (static primitives); `pixelmatch` `allowedMismatchedPixelRatio: 0.001` (via Vitest's own `expect.element`, not the `storybook/test` re-export)
- [x] 5.2 Commit baselines to git next to the stories (the `toMatchScreenshot` default) with a first-run generation path; do NOT store baselines as a CI artifact, so baseline changes are reviewable in the PR diff and never expire (design D4 / spec: "First run establishes baselines") — 4 baselines under `src/components/*/__screenshots__/**/*-chromium-linux.png`, generated with `--update` inside the pinned Playwright container so they match CI
- [x] 5.3 Confirm an intentional visual change is adoptable by regenerating and committing the baseline, and an unintended one fails with a diff image uploaded as a failure artifact (spec: "Visual drift fails the run") — verified in-container: matching render passes; a deliberate glyph swap (music→ticket) FAILS at 0.1%; `--update` re-adopts. Baselines/compare MUST run in the pinned container (OQ2)

## 6. CI integration

- [x] 6.1 Add a `storybook-test` job to `.github/workflows/ci.yaml` reusing the cached Playwright Chromium (`playwright-chromium-*` key); run `vitest --project=storybook` — DEVIATION (D4/OQ2): the job runs INSIDE the pinned `mcr.microsoft.com/playwright:v1.58.1-noble` container (which ships Chromium, so no browser cache is needed) so rendering is byte-deterministic with the committed `toMatchScreenshot` baselines (generated in the same image). Reusing the runner's cached Chromium would make baselines environment-dependent
- [x] 6.2 In the `storybook-test` job, run committed-baseline comparison (no artifact download/upload); on failure upload only the diff image as a failure artifact for inspection (design D4) — the job runs the committed-baseline comparison; on failure it uploads the Vitest HTML report (`storybook-test-report/`), which embeds the `-diff-`/`-actual-` images (Vitest surfaces the diff as a test attachment, not a loose file, so the HTML report is the diff-inspection artifact)
- [x] 6.3 Extend `changes` `paths-filter` for the `storybook-test` job to cover every file category that can change a component's rendered output — `src/components/**`, `**/*.stories.*`, `.storybook/**`, and the test config (`vitest.config.ts`, `.storybook/vitest.setup.ts`) — so a component `.ts`/`.html`/`.css` edit without a story change still triggers the job; add `storybook-test` (+ `storybook-test-skip`) to the `ci-success` `needs`/`allowed-skips` (spec: "Component tests are skipped when no relevant files change" / "CI gate blocks on component-test failure") — added a `storybook` paths-filter output (covers `src/components/**`, `**/*.stories.*`, `.storybook/**` [incl. vitest.setup.ts], `vitest.config.ts`, `package.json`, lockfile, workflows); `storybook-test`/`storybook-test-skip` wired into `ci-success`
- [x] 6.4 Verify a deliberately failing story (render, interaction, a11y, or visual) turns the `ci-success` gate red — verified in-container that each failure class exits `vitest --project=storybook` non-zero: render (AUR0760), interaction (assertion), a11y (color-contrast violation), visual (glyph swap). `storybook-test` is in `ci-success` `needs`; a non-skip `failure` conclusion fails `alls-green` (allowed-skips only permits the `skipped` conclusion). Cannot run live GH Actions from here

## 7. Retire the duplicate visual pipeline

- [x] 7.1 Confirm component-level visual regression is at parity for the design-system components (Risk R6 accepted: page-level route composition remains covered by e2e/smoke, not visual) — component-level `toMatchScreenshot` now covers the design-system primitives (svg-icon, inline-error, state-placeholder, page-header); page-level route composition is accepted as covered by the functional/smoke e2e projects, not by visual (R6)
- [x] 7.2 Remove the Playwright `visual` job (`mobile-visual` project) from `ci.yaml`, its `ci-success` wiring, and `e2e/__screenshots__/` baselines (spec: "A single visual-regression pipeline is maintained") — removed the `visual`/`visual-skip` jobs + `ci-success` wiring, deleted the `mobile-visual` AND `authenticated-visual` projects (both page-level) from `playwright.config.mjs` (+ its now-unused `toHaveScreenshot`/`snapshotPathTemplate`), and `git rm`'d the whole `e2e/visual/**` spec set. No `e2e/__screenshots__` baselines were git-tracked (they were 90-day CI artifacts); only the storybook committed baselines remain → a single visual pipeline

## 8. Documentation

- [x] 8.1 Update `frontend/AGENTS.md` (Stories/Testing sections) with the story-authoring scope, `test-storybook` command, and the projects layout — added a "Component Stories & Testing" section (projects table, story scope + exclusions, a11y/visual specifics, container baseline-regen command) and refreshed the Stack table + essential-commands
