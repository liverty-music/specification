## Tasks

- [x] Update Discover page CTA to navigate directly to Dashboard (`frontend/src/routes/discover/discover-page.ts`): In `onViewSchedule()`, change `onboarding.setStep(OnboardingStep.LOADING)` to `onboarding.setStep(OnboardingStep.DASHBOARD)`, change `router.load('onboarding/loading')` to `router.load('/dashboard')`, update log message from "advancing to loading step" to "advancing to dashboard"
- [x] Remove onboarding timer from loading-sequence (`frontend/src/routes/onboarding-loading/loading-sequence.ts`): Remove `isOnboardingFlow` property, `displayTimer` property, `ONBOARDING_DISPLAY_MS` constant, the `if (this.onboarding.isOnboarding)` branch in `loading()`, the `if (this.isOnboardingFlow)` branch in `attached()`, the `displayTimer` cleanup in `unbinding()`, and unused imports (`OnboardingStep`, `IOnboardingService`/`onboarding` if no longer needed)
- [x] Remove z-index from loading-sequence.css (`frontend/src/routes/onboarding-loading/loading-sequence.css`): Remove `z-index` declarations from `.container::before`, `.pulsing-orb`, `.message-container`, `.step-dots`, `.step-label`, `.progress-label`. Add `isolation: isolate` to root wrapper element.
- [x] Update route guard canLoad (`frontend/src/routes/onboarding-loading/loading-sequence.ts`): Remove the `if (this.onboarding.isOnboarding)` branch in `canLoad()` since onboarding users no longer reach this route. Simplify to redirect onboarding users to dashboard immediately or let authenticated fallback handle it.
- [x] Update specs: Apply deltas from change specs to `specification/openspec/specs/onboarding-tutorial/spec.md`, `specification/openspec/specs/loading-sequence/spec.md`, `specification/openspec/specs/frontend-onboarding-flow/spec.md`
- [x] Tests: Unit test `DiscoverPage.onViewSchedule()` calls correct step/route, unit test `LoadingSequence` no longer has onboarding timer behavior, unit test `LoadingSequence.canLoad()` redirects onboarding users appropriately
- [x] Verification: End-to-end onboarding flow, loading-sequence for authenticated users, localStorage `onboardingStep=2` handling, CSS stacking without z-index
