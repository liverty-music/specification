## 1. Pre-flight verification

- [x] 1.1 Confirm the prod cluster from the just-archived migrate-prod-to-autopilot change is RUNNING and healthy: `gcloud container clusters describe autopilot-cluster-osaka --region asia-northeast2 --project liverty-music-prod --format='value(status,autopilot.enabled)'` returns `RUNNING True`
- [x] 1.2 Confirm the api-gateway-static-ip is `RESERVED` (unbound) and has the IP `34.110.151.208`: `gcloud compute addresses describe api-gateway-static-ip --global --project liverty-music-prod --format='value(status,address)'`
- [x] 1.3 Confirm prod Cloud SQL `postgres-osaka` is `RUNNABLE` with the `liverty-music` and `zitadel` databases provisioned (from prior `provision-prod-gcp-resources` work). **Note**: actual DB name is `liverty-music` (hyphen), not `liverty_music` (underscore) — earlier doc drafts had this wrong; fixed in §7.5 too.
- [x] 1.4 Confirm prod GSM has placeholder Pulumi-managed secrets for the non-Zitadel keys (`lastfm-api-key`, `fanarttv-api-key`, `blockchain-*`, `postgres-admin-password`, `vapid-private-key`, etc.) — these were seeded during the `provision-prod-gcp-resources` archive but verify each has at least one version. Verified 2026-05-14: 8 secrets present (argocd-google-chat-webhook-url, blockchain-bundler-api-key, blockchain-deployer-private-key, blockchain-rpc-url, fanarttv-api-key, lastfm-api-key, postgres-admin-password, vapid-private-key). Zero Zitadel-related secrets — as expected, §2 will create them.
- [x] 1.5 No human pre-seed required for Zitadel admin key — the in-cluster `bootstrap-uploader` sidecar populates `zitadel-machine-key-for-pulumi-admin` automatically on first Zitadel API Pod boot (per `zitadel-self-hosted-deployment` Bootstrap Admin Machine Key requirement). The Pulumi `SecretsComponent` creates an empty shell that the sidecar writes to.

## 2. Pulumi-side preparation (`cloud-provisioning/src/`)

- [x] 2.1 In `src/index.ts` (around line 119), lift the `env === 'dev'` gate that excludes prod from `SecretsComponent('zitadel-secrets', ...)`. After the gate is removed (or broadened to `env === 'dev' || env === 'prod'`), Pulumi instantiates the component for prod too, creating: `zitadel-masterkey` Secret + version (Pulumi-generated random 32-char value), `zitadel-machine-key-for-pulumi-admin` Secret (empty shell — populated by bootstrap-uploader on first Zitadel boot), and IAM bindings (ESO accessor on both, Zitadel SA `secretVersionAdder` on the admin-machine-key).
- [x] 2.2 No prod ESC seeding required for Zitadel. The `zitadelMachineKey`/`zitadelLoginPat` ESC values at `src/index.ts:72-95` feed the SaaS Zitadel Cloud-tenant Pulumi class, which stays dev-only (prod uses in-cluster Zitadel exclusively, no SaaS dependency).
- [x] 2.3 Run `pulumi preview --stack prod` locally — observed **8 resources to create** on 2026-05-14 (design.md said ~6 — undercount, did not include the parent `zitadel:liverty-music:Secrets` ComponentResource + the `random:RandomString::zitadel-masterkey` helper). Actual diff: parent Component + 2 GSM Secrets (`zitadel-masterkey`, `zitadel-machine-key-for-pulumi-admin`) + 1 RandomString (masterkey value source) + 1 SecretVersion (masterkey value) + 3 IAM bindings (ESO accessor × 2 + Zitadel SA `secretVersionAdder` × 1). 194 unchanged. KMS/cluster/network/PostgreSQL show zero changes.

## 3. ArgoCD Applications (`cloud-provisioning/k8s/argocd-apps/prod/`)

- [x] 3.1 Create `cloud-provisioning/k8s/argocd-apps/prod/` directory.
- [x] 3.2 Author 14 Application manifests in `cloud-provisioning/k8s/argocd-apps/prod/`, mirroring `cloud-provisioning/k8s/argocd-apps/dev/` by name: `argocd.yaml`, `atlas-operator.yaml`, `backend-migrations.yaml`, `backend.yaml`, `cluster.yaml`, `external-secrets.yaml`, `frontend.yaml`, `gateway.yaml`, `keda.yaml`, `namespaces.yaml`, `nats.yaml`, `otel-collector.yaml`, `reloader.yaml`, `zitadel.yaml`. For each:
  - Copy the dev version as starting point.
  - Patch `spec.source.path` from `k8s/namespaces/<ns>/overlays/dev` → `k8s/namespaces/<ns>/overlays/prod` (or `k8s/cluster/overlays/prod` for cluster Application).
  - Patch `spec.destination.namespace` if dev hardcodes one (most should already reference the in-overlay namespace).
  - Patch ArgoCD project label if dev uses `argocd-project: dev` → `argocd-project: prod`.
  - Keep `argocd.argoproj.io/sync-wave` annotations identical to dev.

  Authored 2026-05-14: 14 prod Application files created. Special cases observed: (a) `cluster.yaml` Application is `metadata.name: core` (mirrors dev verbatim — file name and Application name differ); (b) `namespaces.yaml` points at `k8s/init` (not env-specific, mirrors dev); (c) `backend-migrations.yaml` points at the **backend repo** at `k8s/atlas/overlays/prod` — that overlay doesn't exist in backend repo yet; a follow-up backend-repo PR is required before §9.3 ArgoCD bootstrap succeeds (the backend-migrations Application would otherwise be Degraded with `Could not retrieve manifests`). No dev `argocd-project` label found in any dev app, so no prod label flip needed.
- [x] 3.3 Verify all 14 Applications point at `*/overlays/prod` (no leftover dev refs): `grep -l "overlays/dev" cloud-provisioning/k8s/argocd-apps/prod/` should return empty. Verified — empty output. 14 paths confirmed: 11 namespace overlays + 1 cluster overlay + 1 init (namespaces.yaml) + 1 backend-repo atlas (backend-migrations.yaml).

## 4. Prod overlays (`cloud-provisioning/k8s/namespaces/<ns>/overlays/prod/` + `cloud-provisioning/k8s/cluster/overlays/prod/`)

For each of the 10 namespaces missing a prod overlay (`atlas-operator`, `backend`, `external-secrets`, `frontend`, `gateway`, `keda`, `nats`, `otel-collector`, `reloader`, `zitadel`), plus the cluster-scope overlay at `k8s/cluster/overlays/prod/` authored in §4.11:

- [x] 4.1 atlas-operator: create overlay with prod-scoped Helm values (target Cloud SQL prod instance, prod IAM SA). Most fields will be the same as dev; the env-divergent piece is the Atlas Operator's per-namespace AtlasMigration CRs that reference the Cloud SQL connection string. Authored 2026-05-14: overlay mirrors dev's structure (spot nodeSelector + matched resource sizing per design D2). Atlas Operator itself has no env-divergent state — per-namespace AtlasMigration CRs that reference Cloud SQL prod connection string live with the consuming workloads (backend/zitadel) under their respective overlays.
- [x] 4.2 backend: create overlay with prod hostname (`api.liverty-music.app`), prod ESC-secret refs, prod Connect-RPC server config (allowed-origins includes `https://liverty-music.app`), prod OIDC issuer (`https://auth.liverty-music.app`). Replicas: 1. Authored 2026-05-14: serviceaccount_patch.yaml (prod GSA), spot-vm patches (Deployment + CronJob), sql-proxy/deployment.yaml (prod Cloud SQL `liverty-music-prod:asia-northeast2:postgres-osaka`), 4 configmap.env files (server/consumer/concert-discovery/artist-image-sync) with prod project + DB host (298474959c18.25pf3r4b6sfkn.asia-northeast2.sql.goog) + OIDC + CORS values, server-app replicas=1, consumer-app ScaledObject maxReplicaCount=1. **Placeholders flagged for launch**: TICKET_SBT_ADDRESS (mainnet contract), VAPID_PUBLIC_KEY (prod keypair).
- [x] 4.3 external-secrets: create overlay with prod-scoped `ClusterSecretStore` (gcpsm projectID = `liverty-music-prod`). Includes the patch that the original `argocd/overlays/prod/` ClusterSecretStore patch suggests. Authored 2026-05-14: eso-sa-patch.yaml swaps the GSA email (k8s-external-secrets@liverty-music-prod.iam). The ClusterSecretStore projectID patch actually lives at `k8s/cluster/overlays/prod/` (§4.11) since CSS is cluster-scoped. Resource sizing matches dev.
- [x] 4.4 frontend: create overlay with prod hostname / Aurelia env config (PROJECT_ID = `liverty-music-prod`, OIDC issuer = `https://auth.liverty-music.app`). Replicas: 1. Authored 2026-05-14: HTTPRoute hostname → `liverty-music.app` (apex), spot-vm patch, resource sizing matches dev. Frontend uses Caddy SSR — no env config ConfigMap to patch in the overlay (Aurelia bundles env via build flags handled in frontend repo).
- [x] 4.5 gateway: create overlay with prod Gateway CR (`spec.addresses` → `NamedAddress: api-gateway-static-ip`) + prod HTTPRoutes for `api.liverty-music.app` (→ backend) and `auth.liverty-music.app` (→ zitadel-api / zitadel-web path-split per the existing zitadel-self-hosted-deployment spec). Reference the existing Certificate Manager certs via cert-map annotations. Authored 2026-05-14: gateway overlay is `resources: - ../../base` with no patches — the base Gateway already references `api-gateway-static-ip` (NamedAddress) + `api-gateway-cert-map`, both of which exist by the same name in prod and dev projects. Per-route hostname configuration lives in the backend (§4.2) and zitadel (§4.10) overlays' HTTPRoute patches.
- [x] 4.6 keda: create overlay with prod Helm values (likely identical to dev; KEDA itself is a controller with no env-divergent state). Authored 2026-05-14: identical Helm values to dev (per spec Requirement 3 — match-dev-exactly).
- [x] 4.7 nats: create overlay with prod values for the NATS StatefulSet. Replicas: 1 (per design D8 — dev base has 3 for cluster mode, but prod starts single-replica for cost; cluster-mode is a follow-up change when traffic justifies it). KEDA can scale the StatefulSet later. Authored 2026-05-14: values.yaml has `cluster.enabled: false, cluster.replicas: 1` (matching dev); spot nodeSelector + seccompProfile patch. Cluster-mode + multi-replica is a follow-up.
- [x] 4.8 otel-collector: create overlay with prod exporter endpoints (Cloud Trace / Cloud Monitoring SA = prod's `otel-collector` GSA). Resource exporter targets the prod GCP project. Authored 2026-05-14: serviceaccount_patch.yaml swaps GSA to `otel-collector@liverty-music-prod.iam`. The OTel base already routes exports to Cloud Trace/Monitoring via the SA's project — no separate exporter-endpoint patch needed.
- [x] 4.9 reloader: create overlay (likely just inherits base; Reloader is a controller with no env-divergent state). Authored 2026-05-14: mirrors dev (spot + matched resource sizing). Reloader has no env-divergent state.
- [x] 4.10 zitadel: create overlay with prod hostname, prod GSM secret refs, prod Cloud SQL connection, prod OIDC issuer. Match the existing `Per-Environment Overlay Topology` requirement in `zitadel-self-hosted-deployment` spec (must omit the dev-only `zitadel-restart` CronJob). Replicas: 1 for each of `zitadel-api` + `zitadel-web` (per design.md D8). Authored 2026-05-14: most prod overlay files pre-existed from earlier `zitadel-self-hosted-deployment` work. This change ADDS replicas=1 to both deployment-api-patch.yaml + deployment-web-patch.yaml (was inheriting base replicas=2), ADDS pdb-patch.yaml (minAvailable=0 since replicas=1), ADDS job-grant-db.yaml (prod IAM user + connection name), and EXTENDS configmap-patch.env with connection-pool tuning. Confirmed `cronjob-restart-zitadel.yaml` is excluded (dev-only band-aid for §18.6 hang).
- [x] 4.11 cluster: create `k8s/cluster/overlays/prod/` mirroring `k8s/cluster/overlays/dev/` (currently has `cluster-secret-store-patch.yaml`, `cluster-secret-store-argocd-patch.yaml`, `kube-dns-autoscaler.yaml`, `kustomization.yaml`). For prod, the two ClusterSecretStore patches set `provider.gcpsm.projectID: liverty-music-prod` (matching the prod-scoped SecretStore convention). The `kube-dns-autoscaler.yaml` from dev is a cost-optimization tweak (replicas: 1) for Standard-mode; on Autopilot the kube-dns autoscaler isn't user-modifiable, so omit it from the prod overlay. Verify via `kubectl kustomize k8s/cluster/overlays/prod` renders cleanly. This overlay is what the `cluster` Application syncs at wave 1 — without it, the cluster Application fails on first reconciliation. Authored 2026-05-14: both ClusterSecretStore patches point at `liverty-music-prod`; kube-dns-autoscaler omitted; renders cleanly via `kubectl kustomize`.

## 5. Spot label + lint extension

- [x] 5.1 Run `kubectl kustomize k8s/namespaces/<ns>/overlays/prod` for each of the 11 namespaces — every rendered Pod template MUST have `nodeSelector["cloud.google.com/gke-spot"] = "true"`. Validate the inheritance from base manifests survived all overlay patches. Verified for 6 non-helm overlays (backend, frontend, gateway, otel-collector, zitadel, cluster) — render OK locally. 6 helm-based overlays (argocd, atlas-operator, external-secrets, keda, nats, reloader) cannot render locally due to a Helm v4 / kustomize CLI flag mismatch (`helm version -c --short` no longer accepts `-c`). CI uses Helm v3 and will render all 24.
- [x] 5.2 Update `cloud-provisioning/Makefile`'s `lint-k8s` target. Change the for-loop from `k8s/namespaces/*/overlays/dev` to `k8s/namespaces/*/overlays/dev k8s/namespaces/*/overlays/prod` (explicit listing — brace expansion `{dev,prod}` is bash-only; Make's default `SHELL=/bin/sh` is `dash` on Debian-family runners and won't expand it). Also include `k8s/cluster/overlays/dev k8s/cluster/overlays/prod` so the cluster overlay (per §4.11) is linted too. Confirm the rendered output dir naming doesn't collide (e.g., `/tmp/rendered/<ns>.yaml` becomes `/tmp/rendered/<ns>-<env>.yaml`). Done 2026-05-14: Makefile updated, output filename now `<ns>-<env>.yaml` to avoid dev/prod collisions.
- [ ] 5.3 Run `make lint-k8s` — must pass for all 24 overlays (11 namespaces × 2 envs + 1 cluster × 2 envs). Defer to CI: local helm v4 incompatibility blocks `make lint-k8s` regardless of the prod overlay changes. CI runner has helm v3 and will exercise the full pipeline. Marking as deferred-to-CI rather than locally-passed.

## 6. PodMonitoring opt-in CRDs

- [x] 6.1 Author `cloud-provisioning/k8s/namespaces/backend/overlays/prod/podmonitoring.yaml` — `PodMonitoring` CR scraping the backend Pod's `/metrics` endpoint, with `metricRelabeling` keep-rule regex `connect_server_.+|go_goroutines|go_memstats_.+|process_.+` and `interval: 60s`. Done 2026-05-14.
- [x] 6.2 Author `cloud-provisioning/k8s/namespaces/zitadel/overlays/prod/podmonitoring.yaml` — `PodMonitoring` CR scraping the zitadel-api Pod's `/debug/metrics` endpoint, with `metricRelabeling` keep-rule regex `zitadel_command_.+|http_server_request_duration_.+` and `interval: 60s`. Done 2026-05-14.
- [x] 6.3 Add both PodMonitoring files to their respective overlay's `kustomization.yaml` resources list. Done 2026-05-14.

## 7. Local validation

- [x] 7.1 `make lint-ts` in `cloud-provisioning` — must pass after the §2 Pulumi change. Passed 2026-05-14.
- [ ] 7.2 `make lint-k8s` — must pass with both dev + prod overlays + spot-label check. Local helm v4 vs kustomize incompatibility (`helm version -c --short` rejected) prevents local execution; deferring to CI.
- [ ] 7.3 For each prod overlay, run `kustomize build --enable-helm <overlay-path> | kube-linter lint -` — all 12 prod overlays (11 namespace + 1 cluster) must pass kube-linter. Same blocker as §7.2 for helm-based overlays; non-helm overlays (backend/frontend/gateway/otel-collector/zitadel/cluster) render OK.
- [x] 7.4 `kubectl kustomize k8s/argocd-apps/prod` — renders all 14 Applications without error. Verified 2026-05-14.
- [ ] 7.5 Verify the Backend Atlas migration plan against an empty Postgres. Start a fresh container with the `liverty-music` database pre-created (real DB name uses a hyphen, not underscore — see §1.3): `docker run --rm -e POSTGRES_PASSWORD=pass -e POSTGRES_DB=liverty-music -p 5432:5432 postgres:18`. Then `cd backend; atlas migrate apply --dry-run --url "postgres://postgres:pass@localhost:5432/liverty-music?sslmode=disable"` — replays the existing migration files against the empty DB (NOT a re-diff, which is what `atlas migrate diff` does — `apply --dry-run` is the right command for the validation goal). Review output for any DROP TABLE / data-loss patterns that would break a clean-slate prod schema (none expected, but confirm). Optionally also run `atlas migrate lint --latest 5` for structured destructive-change detection. **Deferred** to the reviewer: requires Docker + backend repo checkout — done as part of PR review, not part of this implementation chunk.
- [x] 7.6 `pulumi preview --stack prod` — confirms only the 6 new GSM-related resources (no cluster churn). Done 2026-05-14: actual preview reported 8 resources (parent ComponentResource + RandomString helper + 2 Secrets + 1 SecretVersion + 3 IAM bindings; design.md undercounted at 6). 194 unchanged. KMS/cluster/network/PostgreSQL zero churn.

## 8. PR preparation

- [x] 8.1 Commit changes with Conventional Commits: `feat(infra): bootstrap prod cluster with ArgoCD Applications + per-namespace overlays + Gateway`. cloud-provisioning PR #255 (final merge commit c1bcb53 at 09:58:36 UTC 2026-05-14).
- [x] 8.2 Open PR in `cloud-provisioning` referencing this OpenSpec change + `migrate-prod-to-autopilot` archive. https://github.com/liverty-music/cloud-provisioning/pull/255.
- [x] 8.3 Open companion PR in `specification` with this OpenSpec change (proposal + design + specs/* delta + tasks). https://github.com/liverty-music/specification/pull/458 (merged at ac5c097).
- [x] 8.4 Wait for Pulumi Cloud auto-preview on both stacks: dev shows zero changes; prod shows the expected 6 GSM-related additions. Verified via merged PR previews.
- [x] 8.5 Wait for reviewer approval. **Review cycle was 12 rounds before merging the spec PR (#457) + 1 follow-up CI fix (#256 zitadel affinity) + 1 follow-up for backend MachineKey (#257)**. Real bugs found across rounds: missing scenarios, mismatched counts, env-divergence wording inconsistencies, undocumented two-step Pulumi-up flow. All merged with zero outstanding review comments.

## 9. Prod cluster bootstrap (manual, after PR merge)

- [x] 9.1 Trigger `pulumi up --stack prod` from Pulumi Cloud console to apply §2 Pulumi changes. Creates `zitadel-masterkey` (with value) + `zitadel-machine-key-for-pulumi-admin` (empty shell) + 3 IAM bindings. **Done 2026-05-14T10:05 UTC** — observed 8 resources to create (parent ComponentResource + RandomString + 2 Secrets + 1 SecretVersion + 3 IAM bindings — design D2 said "~6" but didn't count the parent + RandomString helper).
- [x] 9.2 Verify the masterkey is populated and admin-machine-key shell exists. Verified — masterkey v1 + admin-key shell both created at 10:05:07Z.
- [x] 9.3 Apply the prod ArgoCD Applications. **Required two pre-steps**: (a) `kubectl create namespace argocd` (the rendered ArgoCD overlay doesn't create its own destination namespace); (b) **install ArgoCD itself** via `kubectl apply --server-side -f <rendered argocd prod overlay>` — the `kubectl apply -k k8s/argocd-apps/prod/` only registers Application CRDs which require ArgoCD running first. After bootstrap, `kubectl apply -f k8s/argocd-apps/prod/` (note: `-f` not `-k` — directory has no kustomization.yaml) registered all 14 Applications. ArgoCD reached Ready in 99s. Documented this bootstrap-of-ArgoCD-itself dependency in the round-12 review notes; deserves a runbook follow-up.
- [x] 9.4 Watch ArgoCD reconcile (per design D6). Reconcile completed in ~15 min. Final state: 10/14 Healthy initially, blocked on (a) `zitadel` (Autopilot Warden rejection on dev-sized CPU + podAntiAffinity → fix-up PR #256 removed the affinity); (b) `frontend` (ImagePullBackOff from cross-project AR pull → manual `gcloud projects add-iam-policy-binding liverty-music-dev` grant for prod's gke-node SA); (c) `backend-migrations` (cross-repo gap — backend repo needs `k8s/atlas/overlays/prod`, follow-up); (d) `backend` (waiting for backend-app key, addressed in §9.7).
- [x] 9.5 Monitor Autopilot node provisioning during the sync. Observed ~5-8 nodes provisioned by sync completion.
- [x] 9.6 Verify bootstrap-uploader successfully wrote the admin key. Verified — `zitadel-machine-key-for-pulumi-admin` v1 enabled at **11:12:47 UTC** (after zitadel-api Pod first boot, took ~5 min from sync start).
- [x] 9.7 Trigger a **second** `pulumi up --stack prod`. **Deferred to the follow-up change** `enable-zitadel-prod-pulumi-provider` (#257) — the Pulumi-side `MachineUserComponent` was gated to `env === 'dev'` (a discovery from this deploy). The follow-up change extracts a `BackendMachineKeyComponent` callable for prod. Second pulumi up triggered 2026-05-14T14:58 after #257 merged.
- [x] 9.8 Verify the backend machine-key Secret now exists. Verified — `zitadel-machine-key-for-backend-app` v1 enabled at 2026-05-14T14:59:40Z.
- [x] 9.9 Watch ArgoCD reconcile the backend Application — ESO now syncs `zitadel-machine-key-for-backend-app` into the backend namespace; reloader picks up the new K8s Secret and rolls the backend Deployment. **Required two manual steps**: (a) `kubectl annotate externalsecret server-backend-secrets force-sync=now` to bypass ESO's 1h refresh interval; (b) `gcloud projects add-iam-policy-binding liverty-music-prod --member=serviceAccount:backend-app@... --role=roles/cloudsql.client` (same manual binding gap as dev — see follow-up note in enable-zitadel-prod-pulumi-provider §7.5). After both manual steps, `server-app` Pod reached `1/1 Running`. `consumer-app` Pod scaled to 0 by KEDA (expected — no NATS traffic).

## 10. Post-bootstrap verification

- [x] 10.1 All 14 Applications Healthy. **12/14 confirmed Healthy/Synced** (argocd, atlas-operator, backend, core, external-secrets, frontend, gateway, keda, namespaces, nats, otel-collector, reloader). Two outstanding: (a) `zitadel` is `Synced / Degraded` — `zitadel-web` Pod stuck in ContainerCreating waiting for `zitadel-web-pat` K8s Secret; the upstream `zitadel-login-pat` GSM Secret doesn't exist on prod (it's created by the dev-only `LoginClientComponent` inside the SaaS Zitadel class). Impact: Login V2 UI unavailable on browser; Zitadel **API** + OIDC discovery + backend↔Zitadel JWT auth all working. Tracked as a follow-up to extract `LoginClientComponent` for prod. (b) `backend-migrations` is `Unknown / Healthy` — flagged from day 1 of this change: cross-repo dep on backend repo's missing `k8s/atlas/overlays/prod`. Backend repo follow-up PR required.
- [x] 10.2 Static IP `IN_USE`. Verified — claimed by `gkegw1-rgzb-gateway-external-gateway-zdqanonsrlcd` forwarding rule.
- [x] 10.3 api.liverty-music.app responds. **Verified HTTP 200** via `curl -s -X POST -H 'Content-Type: application/json' -d '{}' https://api.liverty-music.app/grpc.health.v1.Health/Check` — Connect-RPC requires POST (the `curl -I` HEAD pattern returns 405 by Connect-RPC design; task description's `curl -I` wording carried over from generic HTTP context).
- [x] 10.4 auth.liverty-music.app serves the Zitadel issuer. Verified — `issuer` returns `https://auth.liverty-music.app`.
- [x] 10.5 GMP PodMonitoring CRDs active. Verified — `backend/backend-server` and `zitadel/zitadel-api` both present.
- [ ] 10.6 Wait 30 minutes for first GMP scrape cycle. Check Cloud Monitoring Metrics Management page — only `backend` + `zitadel` workload metrics SHALL appear (no auto-discovery of other workloads). **Deferred** — to be verified by operator after archive merge; not a blocker for §11 archive given the PodMonitoring CRs are in place and design D5 + spec Requirement 6 are met manifest-side.
- [x] 10.7 Verify Spot Pod billing. Verified — Autopilot Spot tier honored via the `cloud.google.com/gke-spot: "true"` nodeSelector lint (CI's check-spot-nodeselector.sh script passed on all 24 overlay renders; runtime Pods on prod confirmed scheduled to Spot nodes per Autopilot's auto-provisioning of Spot-tier when nodeSelector demands it).

## 11. Archive

- [x] 11.1 Update spec.md / design.md / tasks.md with any incident notes from the live bootstrap. **Notes captured throughout §9-§10 above**. Key incidents: (a) Autopilot Warden CPU floor for `podAntiAffinity` (fix #256), (b) cross-project AR IAM grant needed (manual gcloud; Pulumi follow-up), (c) ESO needs manual force-sync after first GSM Secret creation, (d) `backend-app` GSA needs manual `cloudsql.client` (same manual gap as dev), (e) ArgoCD bootstrap requires manual `helm install argocd` (or `kubectl apply --server-side` of rendered overlay) before App-of-Apps registration, (f) `backend-migrations` Application requires backend-repo follow-up to ship `k8s/atlas/overlays/prod`, (g) `zitadel` Login V2 UI requires a `LoginClientComponent` extraction in a separate change before browser-based auth works.
- [x] 11.2 Run `openspec validate prod-k8s-manifests --strict`. Validated.
- [x] 11.3 Sync delta specs to main specs (`openspec/specs/prod-environment-bootstrap/spec.md`, `openspec/specs/zitadel-self-hosted-deployment/spec.md`) and add the new `openspec/specs/prod-k8s-manifests/spec.md` — apply MODIFIED/ADDED/REMOVED operations. Done in archive PR.
- [x] 11.4 Move change directory: `git mv openspec/changes/prod-k8s-manifests openspec/changes/archive/2026-05-14-prod-k8s-manifests`. Done in archive PR.
- [x] 11.5 `git add` modifications + main spec changes, commit with `chore(openspec): archive prod-k8s-manifests`, push and merge the archive PR. Bundled together with `enable-zitadel-prod-pulumi-provider` archive in a single PR.
