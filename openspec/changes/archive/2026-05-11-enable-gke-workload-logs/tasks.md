## 1. PR — cloud-provisioning: enable WORKLOADS in dev GKE loggingConfig

- [x] 1.1 In `cloud-provisioning/src/gcp/components/kubernetes.ts`, change `loggingConfig.enableComponents: ['SYSTEM_COMPONENTS']` to `['SYSTEM_COMPONENTS', 'WORKLOADS']`. Rewrite the surrounding comment to explain *why* logging now permits `WORKLOADS` (log-based alerts require workload stdout) but `monitoringConfig` stays system-only (no metric-based workload alerts today).
- [x] 1.2 Run `make check` in `cloud-provisioning/`; biome + tsc + vitest all pass.
- [x] 1.3 Run `pulumi preview --stack dev`; expected diff: a single `~ gcp:container/cluster:Cluster` update with `loggingConfig.enableComponents: [SYSTEM_COMPONENTS] => [SYSTEM_COMPONENTS, WORKLOADS]`. Confirm no other unrelated changes are bundled. *(Confirmed: `~ 2 to update / 231 unchanged`. The cluster diff is the targeted `+ [1]: "WORKLOADS"` addition. The second `~` is the `dashboard-zitadel-observability` dashboardJson cosmetic drift (etag + default xPos/yPos fields populated by GCP server-side, pre-existing from before this change). No destructive ops, no resource creation.)*
- [x] 1.4 Open cloud-provisioning PR titled `fix(gke): enable workload logging so log-based alerts can fire`. Cross-link this openspec change in the PR body. The PR body should explicitly call out the silent-non-functional alert backlog (`alert-error-log-server` + 4 others, plus `alert-backend-jwt-zitadel-errors` from `zitadel-observability`). *(Opened as cloud-provisioning #235.)*
- [x] 1.5 Merge after CI green. Pulumi Cloud Deployments auto-applies on merge. *(Merged 2026-05-11 as cloud-provisioning a01f9e7. One follow-up commit during review: `5003fef refactor(gke): drop fix-narration from loggingConfig comment` per claude[bot] inline review.)*

## 2. Post-merge verification

- [x] 2.1 Confirm the cluster config reflects the change: `gcloud container clusters describe standard-cluster-osaka --zone=asia-northeast2-a --project=liverty-music-dev --format='value(loggingConfig.componentConfig.enableComponents)'` returns `SYSTEM_COMPONENTS;WORKLOADS`. *(Confirmed post-merge 2026-05-11.)*
- [x] 2.2 Confirm workload logs are flowing: `gcloud logging read 'resource.type="k8s_container" AND resource.labels.namespace_name="backend"' --project=liverty-music-dev --freshness=15m` returns recent entries. If the cluster's managed logging agent hasn't rolled out within ~15 min, force the maintenance via `gcloud container clusters update standard-cluster-osaka --zone=asia-northeast2-a --logging=SYSTEM,WORKLOAD --no-async` (idempotent — the same target state). *(Confirmed: `consumer-app` stdout entries appearing in Cloud Logging within ~2 min of update 285 completing. No manual agent rollout needed.)*
- [x] 2.3 Re-run the `zitadel-observability` §1.13 smoke-test (deferred at archive time): apply the synthetic-ERROR pod manifest, temporarily lower the JWT alert threshold via `gcloud alpha monitoring policies update`, confirm Slack + Google Chat receive the notification, revert threshold. Document timestamps + screenshots in this change's tasks file for the archive. *(Confirmed 2026-05-11 06:58–07:01 UTC. Sequence: synthetic ERROR pod `smoke-test-jwt-log` emitted 5 entries with `labels.k8s-pod/app="server"` matching the LogMetric filter; JWT alert threshold lowered to `0.01666/sec` over `60s`; Google Chat `alert-backend-dev` channel received an OPEN notification "Backend JWT validation error rate ... is above threshold of 0.017 with a value of 0.067" within the expected window; threshold reverted to `0.166667/sec, 300s`; auto-close notification "returned to normal with a value of 0.000" delivered after metric returned to zero. Smoke pod cleaned up. End-to-end notification path (LogMetric → AlertPolicy → Google Chat) fully verified — first time since the cluster was provisioned. Slack channel verified by transitivity since both Slack and Google Chat were on the same `notificationChannels` list and Google Chat received the page.)*
- [x] 2.4 Verify legacy log-based alerts are now armed by triggering one with a deliberate test ERROR log (or wait for organic; whichever lands first). Confirm one of `alert-error-log-server` / `alert-error-log-consumer` / `alert-atlas-migration-failure` fires correctly on a real or synthetic ERROR. *(Verification uncovered a second-layer bug: `MonitoringComponent` was passing wrong `clusterLocation` (region instead of zone) and wrong `clusterName` (`cluster-osaka` instead of `standard-cluster-osaka`), so none of the 5 alerts in that component had ever matched a log entry — even after #235 enabled workload log ingestion. Fixed in cloud-provisioning #236 (merged 2026-05-11 as fb5679e). Confirmed 2026-05-11 ~08:20 UTC: synthetic `smoke-test-server-error` pod emitted one matching ERROR log; `alert-error-log-server` fired and delivered a notification to Google Chat `alert-backend-dev` with all labels correctly extracted (cluster_name=standard-cluster-osaka, location=asia-northeast2-a, pod_name=smoke-test-server-error, error_code=SMOKE_TEST, rpc_method=synthetic). By transitivity through the same MonitoringComponent code path, the four sibling alerts — Consumer / Concert Discovery ERROR Log, Poison Queue Message, Atlas Migration Failure — are now functional.)*

## 3. Cost monitoring (light-touch)

- [ ] 3.1 One week post-deploy, check Cloud Logging billing line in `liverty-music-dev` GCP billing for the delta. Expected < 1 GiB/month for `k8s_container` logs. If meaningfully higher, file a follow-up change to add `LoggingExclusion` filters for noisy pods (e.g., cloud-sql-proxy chatty connection logs). *(Deferred — calendar wait: re-check 2026-05-18 or later. Cloud Logging billing data has 24–48h lag; budget threshold data needs ~1 week of post-merge traffic to be meaningful. Non-blocking for archive.)*
- [ ] 3.2 Confirm `gcp-cost-guardrails` billing budget thresholds still hold (50/90/100% of ¥3,000/month for dev). No action needed if not crossed. *(Deferred — same calendar wait as §3.1. Cloud Billing Budget API is currently disabled in `liverty-music-dev`; enable + query at the 1-week mark.)*

## 4. Archive

- [x] 4.1 Run `/opsx:verify enable-gke-workload-logs` after §2 verification passes. *(Run 2026-05-11. Verdict: no CRITICAL issues. One WARNING flagged §2.4 transitive-only verification at the time, which is now fully resolved via the §2.4 smoke-test + #236 filter fix.)*
- [x] 4.2 Add a footer reference to the archived `zitadel-observability/tasks.md` §1.13: "*§1.13 follow-up landed in `enable-gke-workload-logs` (PR-X, merged YYYY-MM-DD)*" — close the dangling deferred task. *(Done: §1.13 in the archived `2026-05-03-zitadel-observability/tasks.md` is now ticked and carries a back-reference to this change + #235 + #236.)*
- [x] 4.3 Run `/opsx:archive enable-gke-workload-logs` to fold the spec delta into main `openspec/specs/gke-standard-infrastructure/spec.md`. *(Run 2026-05-11. Folded the MODIFIED requirement "Dev cluster SHALL disable Google Managed Prometheus" into main spec; old title kept (was tweaked back at archive time to preserve the existing requirement identity). Resulting requirement text reflects WORKLOADS in logging while keeping monitoring system-only.)*
- [ ] 4.4 Open archive PR; merge after CI green.
