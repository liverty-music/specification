## 1. Remove the raw code from the presentation entity

- [x] 1.1 In `frontend/src/entities/concert.ts`, remove the `adminArea?: string` field from the `Concert` interface.
- [x] 1.2 In `frontend/src/adapter/rpc/mapper/concert-mapper.ts`, keep the local `adminArea` variable only to derive `locationLabel`; remove `adminArea,` from the returned entity object.

## 2. Consume only the display label in the detail sheet

- [x] 2.1 In `frontend/src/components/live-highway/event-detail-sheet.html`, bind the venue secondary line to `${event.locationLabel}` with `if.bind="event.locationLabel"`.
- [x] 2.2 In `frontend/src/components/live-highway/event-detail-sheet.ts`, change `googleMapsUrl` to use `event.locationLabel` for the area term instead of `displayName(event.adminArea)`, and remove the now-unused `displayName` import.

## 3. Tests

- [x] 3.1 In `test/adapter/rpc/mapper/concert-mapper.spec.ts`, remove the `adminArea` assertions (keep the `locationLabel` assertions for both the known-code and missing-admin-area cases). Also dropped the stale entity-level `adminArea` from `test/helpers/mock-date-groups.ts`.
- [x] 3.2 In `test/components/live-highway/event-detail-sheet.spec.ts`, drop `adminArea` from the `makeEvent` mock and update the `googleMapsUrl` cases to drive behavior via `locationLabel` (present → `venue + label`, empty → `venue` only).

## 4. Verify

- [x] 4.1 Run `make check` (lint + typecheck + unit tests) in `frontend/` and confirm green — typecheck proved no remaining references to the removed `adminArea` field. Result: 105 files / 1152 tests passed, 2 skipped; lint/format clean.
- [x] 4.2 Visual baseline regenerated by main-branch CI on the v1.6.0 merge (PR #414).
- [x] 4.3 Shipped in v1.6.0 — venue line renders the localized prefecture name (`locationLabel`), not the raw `JP-13` code.
