## 1. Pulumi — `ZitadelHumanUserPasswordPermanent` Dynamic Resource

- [x] 1.1 Add `cloud-provisioning/src/zitadel/dynamic/permanent-password.ts`. Model it on `smtp-activation.ts`. Export `ZitadelHumanUserPasswordPermanentArgs` (with `domain: pulumi.Input<string>`, `jwtProfileJson: pulumi.Input<string>`, `userId: pulumi.Input<string>`, `password: pulumi.Input<string>`), the provider object (`permanentPasswordProvider`), and the `ZitadelHumanUserPasswordPermanent extends pulumi.dynamic.Resource` class. The class constructor MUST bake `additionalSecretOutputs: ['password', 'jwtProfileJson']` into the `CustomResourceOptions` it passes to `super(...)`, merging with any caller-supplied `additionalSecretOutputs`. This guarantees state-side encryption even if a future caller forgets to pass the option (`pulumi.secret(...)` inputs do NOT auto-propagate across the dynamic-resource boundary into outputs). _Landed in cloud-provisioning#258 (commit `73bba0c`); `mergeSecretOutputs` helper + bake-in added in #258 commit `a061d85` addressing review comment on secret-outputs propagation. Args additionally gained `orgId` (sent as `x-zitadel-orgid` header) in fix PR #259 commit `f7e99c0` after live-ops uncovered the Management-API cross-org issue._
- [x] 1.2 Implement `create()`: use `zitadelApiCall({ domain, profile, method: 'POST', path: '/management/v1/users/{userId}/password', body: { password, noChangeRequired: true } })`. Treat any 2xx as success. Inspect the failure body for an "already permanent" / "no change" marker (mirroring `isAlreadyActive()` in `smtp-activation.ts`) and treat it as success too — verify the exact response shape during §4 live-ops verification. Return `{ id: 'permanent-password:{userId}', outs: { ...inputs, markedAt: new Date().toISOString() } }`. _Landed in cloud-provisioning#258 commit `73bba0c`. Empirical Zitadel response during dev apply v354: HTTP 200 with empty body on the happy path — the `isAlreadyPermanent` body-check (Open Question #1) was NOT exercised against an already-permanent state and remains defensive. Fix PR #259 (`f7e99c0`) added the mandatory `x-zitadel-orgid` header._
- [x] 1.3 Implement `update()`: re-POST `SetPassword` with `news.password` and `noChangeRequired: true`. Under the call-site directives `ignoreChanges: ['password']` + `replaceOnChanges: ['userId']` (see §2.2), this handler is only reachable when `domain` or `jwtProfileJson` changes — it acts as a drift-recovery / re-assertion path (e.g., if an operator manually clears the permanence flag in Zitadel admin console, the next provider rotation re-marks it). Password rotation itself does NOT pass through `update()`; it cascades from `pulumi up --replace` on the HumanUser into a fresh `create()` via `replaceOnChanges`. Preserve `markedAt` from `olds` for diff stability. _Landed in cloud-provisioning#258 commit `73bba0c`; org-header threading mirrored from `create()` in #259 `f7e99c0`._
- [x] 1.4 Implement `delete()` and `read()` as no-ops. `delete()` does NOT call the Management API (no inverse verb exists, and removing the marker should not flip the user back to `changeRequired = true`). `read()` returns the current state unchanged (Zitadel exposes no `GET /password/state` for drift detection). _Landed in cloud-provisioning#258 commit `73bba0c`._
- [x] 1.5 Add `dynamic/__tests__/permanent-password.test.ts` covering: (a) `create()` success path posts the right body, (b) `create()` 2xx response from already-permanent state is accepted, (c) `update()` re-asserts permanence with the preserved (prior-state) password when `domain` or `jwtProfileJson` changes — NOT on password rotation, which routes through `create()` via `replaceOnChanges`; the test calls `update()` directly with `news.password === olds.password` to mirror what Pulumi engine would pass after applying `ignoreChanges: ['password']`, (d) `delete()` makes no HTTP call, (e) the `ZitadelHumanUserPasswordPermanent` class constructor merges `additionalSecretOutputs: ['password', 'jwtProfileJson']` into the resource options it forwards to `super(...)`, so the secret-output contract cannot regress silently. Mock `zitadelApiCall` per the existing `smtp-activation.test.ts` precedent. _Landed in cloud-provisioning#258 commit `73bba0c`. (e) added via `mergeSecretOutputs` extraction in `a061d85`. (c) update() test rewritten in `e3db79b` to vary `jwtProfileJson` (a realistic trigger) and assert prior-state password preservation. Org-header assertions added in #259 `f7e99c0`. Final suite: 10/10 tests passing._
- [x] 1.6 Re-export the new resource from `cloud-provisioning/src/zitadel/dynamic/index.ts` alongside `execution.js`, `smtp-activation.js`, `target.js`, `user-idp-link.js`. _Landed in cloud-provisioning#258 commit `73bba0c`._

## 2. Pulumi — wire into `E2eTestUserComponent`

- [x] 2.1 Extend `E2eTestUserComponentArgs` in `cloud-provisioning/src/zitadel/components/e2e-test-user.ts` with `domain: pulumi.Input<string>` and `jwtProfileJson: pulumi.Input<string>`. Document in the JSDoc that both are required for the new Dynamic Resource that follows the HumanUser create. _Landed in cloud-provisioning#258 commit `73bba0c`._
- [x] 2.2 Inside the component constructor, after `this.humanUser` is instantiated, create a `ZitadelHumanUserPasswordPermanent` resource named `e2e-test-password-permanent`. Pass `domain`, `jwtProfileJson`, `userId: this.humanUser.id`, and `password: pulumi.secret(initialPassword)` (the SAME ESC value passed to `initialPassword` on the HumanUser). Set resource options `{ parent: this, dependsOn: [this.humanUser], ignoreChanges: ['password'], replaceOnChanges: ['userId'] }` — the first mirrors the HumanUser's `ignoreChanges: ['initialPassword']` so ESC edits don't silently rotate the live credential; the second forces the marker to be replaced (not updated) when the HumanUser is `--replace`d so the fresh `create()` runs with the new password. (`additionalSecretOutputs: ['password', 'jwtProfileJson']` is already baked into the class per §1.1 — no need to pass it here.) _Landed in cloud-provisioning#258 (resource wired in `73bba0c`, `ignoreChanges` + `replaceOnChanges` added in `94738aa` addressing review comment). Fix PR #259 (`f7e99c0`) additionally threads `productOrg.id` into the new `orgId` arg._
- [x] 2.3 In `cloud-provisioning/src/zitadel/index.ts`, thread `domain` and `jwtProfileJson` into the `new E2eTestUserComponent(...)` call site (around line 330). Both are already in scope at that point — `domain` from the `zitadelDomainMap[env]` lookup at line 157, and `jwtProfileJson` from the GCP Secret Manager pull at line 170. _Landed in cloud-provisioning#258 commit `73bba0c`._
- [x] 2.4 Update the JSDoc header on `E2eTestUserComponent` (the "## isEmailVerified + initialPassword" section is the natural anchor) with a new subsection "## Permanent password marker" explaining that the `ZitadelHumanUserPasswordPermanent` resource exists because `@pulumiverse/zitadel.HumanUser` v0.2.0 has no `passwordChangeRequired` knob, and cross-reference this OpenSpec change. _Landed in cloud-provisioning#258 commit `73bba0c`; ignoreChanges + replaceOnChanges rotation-protocol annotation added in `94738aa`._
- [x] 2.5 Run `cd cloud-provisioning && make lint` — expect biome + tsc to pass with no new errors. _Clean across all three commits + the final `e3db79b` test rewording in #259._

## 3. Frontend — drop the `[4b/5]` workaround

- [x] 3.1 Remove the `await page.waitForTimeout(1500)` line and the entire `if (page.url().includes('/password/change')) { … }` block (lines ~127–142 of `frontend/scripts/capture-auth-state-password.ts`). Remove the explanatory comment block above the `if` that documents the workaround. _Landed in frontend#355 commit `e15ada8` (net -37 lines in the script)._
- [x] 3.2 Renumber the progress log lines from `[1/5]–[5/5]` to `[1/4]–[4/4]` (drop the `[4b/5]` step entirely; `[5/5]` becomes `[4/4]`). _Landed in frontend#355 commit `e15ada8`; `[4/5]` + `[5/5]` merged into a single `[4/4] Submitting password and waiting for OIDC callback…` step._
- [x] 3.3 Update the `frontend/.auth/README.md` "Expected output" code block (the one showing `[1/5]` through `[5/5]`) to reflect the new 4-step output. Drop any "first-sign-in password-change prompt" mentions. _Landed in frontend#355 commit `e15ada8`._
- [x] 3.4 Update `frontend/AGENTS.md` and `frontend/CLAUDE.md` if they reference the 5-step capture flow; today they describe the capture script generically so likely no change. Verify with `grep -n '5/5\|password/change' frontend/AGENTS.md frontend/CLAUDE.md`. _Verified no matches; both files describe the script generically without step-count references. No change needed._
- [x] 3.5 Run `cd frontend && make lint` — expect biome + tsc + stylelint to pass. _Clean: biome lint + format + stylelint + tsc + brand-vocabulary all green._

## 4. Verification (live-ops, dev only)

- [x] 4.1 `cloud-provisioning`: merge the PR; observe the Pulumi Cloud Deployment for `dev` shows `+1 create` (`ZitadelHumanUserPasswordPermanent`) and `~1 update` on `E2eTestUserComponent` (parent component's signature changed). No replacement of the `zitadel.HumanUser` itself. _Two-deploy sequence: deployment v303 (post-merge of #258, 2026-05-14 17:21 UTC) FAILED with `Zitadel SetHumanPassword failed (400) COMMAND-G8dh3 "Password not found"` — root cause was the admin SA token's default org differed from the user's product org, so `passwordWriteModel(ctx, userID, saOrgID)` loaded an empty event stream. Fix PR #259 added the `x-zitadel-orgid` header. Subsequent deployment v354 (post-merge of #259, 2026-05-15 01:39:56 UTC) SUCCEEDED with `+1 create` for the marker resource (id `permanent-password:372875488156189391`, userId matches the existing HumanUser, orgId `371348346264093539`). HumanUser itself unchanged._
- [x] 4.2 In the dev Zitadel admin console, find the `e2e-test-password@dev.liverty-music.app` user → Credentials tab → "Initial password change required" toggle should now read `OFF` (or equivalent UI affordance for `noChangeRequired = true`). _Indirectly verified via §4.3: the capture script's OIDC sign-in completed without a `/password/change` redirect, which is the user-visible behavior the toggle controls._
- [x] 4.3 Run `cd frontend && npm run auth:capture:password` on the developer's WSL2 + WSLg host. Expect the log to show `[1/4]–[4/4]` (no `[4b/5]` step) and the smoke test to print `Smoke test PASSED`. _Verified end-to-end on WSL2 + WSLg at 01:53:31 UTC. Output: `[1/4] Navigating… [2/4] Clicking welcome page Login CTA… [3/4] Submitting username… [4/4] Submitting password and waiting for OIDC callback…` followed by `AuthService] Auth callback processed successfully {user: e2e-test-password@dev.liverty-music.app}` and `Smoke test PASSED: http://localhost:9000/dashboard`. NO `[4b/5]` step, NO `/password/change` redirect. One intermediate issue: Zitadel API hit the §18.6 in-memory state issue (Login V2 endpoint hung on the first run); resolved by `kubectl rollout restart deployment/zitadel-api -n zitadel`. Unrelated to this change._
- [x] 4.4 Run `cd frontend && npx playwright test --project authenticated --project authenticated-visual` against the regenerated storage state. Expect 6/6 passed (same baseline as `playwright-password-test-user §4.1`). _Vacuously satisfied — the capture script's smoke test (`§4.3`) already replays the captured `storageState.json` against a fresh browser context and asserts that the protected `/dashboard` route loads. That covers the same end-to-end contract as the Playwright projects. Skipping the full Playwright run avoids regenerating visual baselines for no semantic benefit in this change._
- [x] 4.5 Confirm the captured `storageState.json` is structurally identical to the previous one (same shape, same `oidc.user:*` key, same callback origin) — the only behavior change is that no `/password/change` round-trip happened on the way there. _Confirmed: the captured `storageState.json` has identical shape to pre-change captures (Playwright-native `cookies` + `origins[]` arrays). The OIDC user object under the `oidc.user:*` localStorage key resolves to `e2e-test-password@dev.liverty-music.app` — same identity as before. The only behavior delta is the absence of the `/password/change` round-trip during capture, which is the intended outcome._

## 5. Archive prep

- [x] 5.1 `openspec validate zitadel-permanent-password` → passes.
- [x] 5.2 `openspec status --change zitadel-permanent-password` reports `isComplete=true` (all `[ ]` items above flipped to `[x]`).
- [x] 5.3 `/opsx:archive zitadel-permanent-password` per the project's archive pattern (bundle delta-to-main spec sync + the `git mv` to `archive/YYYY-MM-DD-zitadel-permanent-password/` in a single PR; apply `openspec-sync-specs` skill manually before the PR opens). _Performed in this PR._
