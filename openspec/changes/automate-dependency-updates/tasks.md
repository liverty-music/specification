## 1. Dead-pin cleanup

- [x] 1.1 In `frontend`, land the already-staged removal of `overrides.bfj` on a branch (it is currently uncommitted on `main`). Verify `npm ci` reports no lockfile change and `npm audit --omit=dev --audit-level=moderate` reports 0 vulnerabilities.
- [x] 1.2 In `frontend`, attempt removal of `overrides.minimatch`: delete the entry, regenerate `package-lock.json` on Node 22 (matching CI), and confirm `filelist` resolves without introducing an advisory. If `filelist`'s declared `^5.0.1` now resolves to a version that breaks the build or trips the audit, keep the override and record why in a comment on the lockfile-adjacent PR description instead.
- [x] 1.3 Verify the top-level `liverty-music/e2e/` husk is gone and that `frontend/e2e/` is untouched (deleted during exploration; confirm no tooling referenced the old path).

## 2. CI hardening — WebKit coverage

- [x] 2.1 Add `--project=webkit-repro --project=chromium-control` to the `e2e` job in `frontend/.github/workflows/ci.yaml`. The job already installs both `chromium` and `webkit`, so no new browser install step is needed.
- [x] 2.2 Confirm `page-help-sheet-webkit.spec.ts` now executes in CI and that the bottom-sheet regression guard it encodes passes.
- [x] 2.3 Audit every project in `playwright.config.mjs` against the `ci-optimization` requirement that each is either executed or documented as unrunnable. `authenticated` needs a `storageState` CI has no way to produce — record the reason at the project.
- [x] 2.8 Repair the two `it.skip` cases in `frontend/test/app-shell.spec.ts` (landing-page render, layout navigation/viewport). These are the assertions an Aurelia upgrade would most plausibly break, and Aurelia automerge is gated on them (design D17). Promoted from a follow-up: the governing rule applies here identically to the workbox gate.
- [x] 2.5 Re-verify the `pwa-offline-cache.spec.ts` exclusion. Its recorded reason is "Requires Service Worker + offline — not available in CI headless", but the spec drives offline through `context.setOffline()`, a core Playwright API that works in headless Chromium, and Playwright supports Service Workers there. Remove it from the `pwa` project's `testIgnore` and run it in CI. If it passes, the gap that would leave `workbox` unverified is closed. If it genuinely cannot run, record the actual failing capability — not the inherited claim — and name the control covering the gap (design Risk 2).
- [x] 2.6 Note that `pwa-offline-cache.spec.ts` uses `waitForTimeout(3000)` / `waitForTimeout(5000)` in several places. If enabling it in CI produces flakes, replace the fixed waits with explicit conditions rather than re-excluding the spec — re-exclusion would reopen the gap 2.5 just closed.
- [x] 2.7 Leave `pwa-install-prompt.spec.ts` excluded: `beforeinstallprompt` depends on browser install heuristics that headless CI cannot trigger. Record it as an enumerated gap whose scope is `vite-plugin-pwa` manifest behavior, not workbox caching.
- [x] 2.4 Audit at SPEC granularity, not just project granularity. `pwa-offline-cache.spec.ts` and `pwa-install-prompt.spec.ts` are specs inside the `pwa` project's `testIgnore`, not projects of their own, so a project-level audit misses them entirely. Enumerate every spec matched by no executed project, confirm each carries its reason, and publish the resulting list as the known-gaps set that design Risk 2 refers to.

## 3. CI hardening — cloud-provisioning verification

- [x] 3.1 Add a `test` job to `cloud-provisioning/.github/workflows/ci.yml` running the repository's `vitest` suite, with its matching `test-skip` job, and add both to `ci-success`'s `needs` and `allowed-skips`.
- [x] 3.1a Extend the `changes` paths-filter in that workflow to include `Pulumi.yaml` and `Pulumi.*.yaml`. Without them a PR that only edits stack configuration skips every job and still reports `CI Success` green, violating the "preview on every pull request" requirement.
- [x] 3.2 Commit the Pulumi Cloud deployment settings as `cloud-provisioning/Pulumi.dev.deploy.yaml` and `Pulumi.prod.deploy.yaml` (`pulumi deployment settings pull -s <stack>`). They currently exist only in the Pulumi Cloud console, so the trigger `paths` that carry the D6 control are neither reviewable nor diffable — and `docs/runbooks/pulumi-state-recovery.md` already cites `Pulumi.dev.deploy.yaml` as though it were in the repository. Confirm the committed files round-trip (`pull` produces no diff) before relying on them.
- [x] 3.3 Add `Pulumi.prod.yaml` to the prod stack's trigger `paths`. It is currently `src/**` alone, so a pull request editing only prod stack configuration changes what would be deployed and is previewed by nothing. Do NOT build a `pulumi-preview` job in GitHub Actions: `previewPullRequests: true` is already set on both stacks, a second preview would duplicate it, and it would require a stored `PULUMI_ACCESS_TOKEN` where OIDC already works (design D6).
- [x] 3.3b Record, inline in both `*.deploy.yaml` files next to `paths`, that dependency manifests (`package.json`, `package-lock.json`) are deliberately excluded and why — previewing a Renovate pull request would `npm ci` and execute an unread third-party package with ESC-resolved production credentials, and `skipInstallDependencies` is `false`. Today this holds only because `paths` happens not to list them; the comment is what turns an accident into a control (spec: "The exclusion holds only by coincidence"). Verify no GitHub Actions condition is relied on for this — a workflow `if:` cannot govern a Pulumi Cloud trigger.
- [x] 3.3a Update `.github/pull_request_template.md`: its Pulumi Preview section says "check the CI/GitHub Actions output ... If CI is not running, paste the local output". The preview runs, but in Pulumi Cloud rather than GitHub Actions, so the instruction points reviewers at the wrong place AND implies preview is optional. Point it at the Pulumi Cloud PR comment and drop the local-paste fallback.
- [ ] 3.5 Verify against the real mechanism: on a member-authored no-op PR the preview reports zero changes; on a deliberately resource-altering PR it reports the change; on a PR editing only `Pulumi.prod.yaml` the preview now runs (it did not before 3.3); and a Renovate PR in `cloud-provisioning` reaches `CI Success` with NO preview having run. Confirm prod still has no `deployCommits` (no apply on merge) after the settings round-trip.
## 4. CI hardening — Playwright lockstep

- [x] 4.1 Change `frontend/package.json` `@playwright/test` from `^1.49.1` to the exact currently-resolved version (`1.58.1`) and regenerate the lockfile on Node 22.
- [x] 4.2 Confirm the value matches the `mcr.microsoft.com/playwright:v1.58.1-noble` container tag in `ci.yaml`, and that `npm ci` plus the Storybook component tests still pass against the committed baselines.

## 5. Renovate — organization preset

- [x] 5.1 Create `liverty-music/.github/renovate-config.json` extending `config:best-practices` (design D14). Do NOT assemble the configuration from scratch — the preset supplies the release-age delay, Action SHA pinning, Docker digest pinning, dev-dependency pinning, lock file maintenance, abandonment detection and config migration, plus the maintained groupings D3 defers to.
- [x] 5.1b Add the project-specific layer on top: schedule (weekly Saturday JST groups, monthly groups at month start), `prConcurrentLimit`, `dependencyDashboard: true`, and **`automerge` globally off** for now.
- [x] 5.1c Configure the release-age delay explicitly rather than relying on the preset default, so it survives a schedule change. `security:minimumReleaseAgeNpm` covers npm only; extend the delay to the other automerged datasources — Go modules, container images and GitHub Actions — so the spec's scope and the configuration agree. Verify the interaction with the vulnerability-remediation path that bypasses the schedule: that path must not silently bypass the delay as well, and any exemption must carry a recorded reason (spec: "Releases are not adopted until they have aged").
- [x] 5.1d In the org preset, disable automerge for the `.github` repository specifically and permanently (design D11). `.github` has no `GitHubRepositoryComponent`, so no branch protection and no `CI Success` gate exist there — an automerge would land with no gate at all. Renovate still raises PRs for its GitHub Actions.
- [x] 5.1e Set Renovate to rebase its pull requests when the base branch moves, so `cloud-provisioning`'s `requireUpToDateBranch: true` does not deadlock the automerge queue (design D13). Do NOT remove that flag — it prevents a documented class of Pulumi resource-deletion accident.
- [x] 5.2 Confirm which managers the preset does not already cover, and add rules ONLY for those. Outcome: none were needed. Renovate ships native managers for mise, pre-commit and kustomize, so writing custom ones would have violated the capability's own rule that maintained upstream configuration is reused rather than reimplemented. `buf.lock` is the exception in the other direction — no manager or datasource exists for the Buf Schema Registry, so it cannot be covered at all and is recorded as a known gap with `buf dep update` as its named control (see the spec, and task 11.3).
- [x] 5.2a Pin the mise tools to concrete versions first. `cloud-provisioning/.mise.toml` declares `kustomize = "latest"` and `specification/.mise/config.toml` declares `buf = "latest"` and `pre-commit = "latest"`; a floating reference can never produce an update proposal, so the mise axis would be silently inert and task 9.2 would misread that as "no updates available".
- [x] 5.3 Determine whether `hostRules` credentials are needed for the `buf.build/gen/npm/v1/` registry. Outcome: they are not. The registry answers unauthenticated — verified directly — and `frontend/.npmrc` carries no token, so there is no credential to supply. Nothing was added.
- [ ] 5.4 Install the Mend-hosted Renovate GitHub App on the `liverty-music` organization and confirm each repository resolves the preset (the onboarding PR should show `local>liverty-music/.github:renovate-config` in its inherited config).

## 6. Renovate — per-repository configuration

- [x] 6.1 `backend/renovate.json`: extend the org preset; add the `go-tools` group only; set `constraintsFiltering: strict`; add `postUpdateOptions: ["gomodTidy"]` so `go.mod`/`go.sum` stay tidy across bumps; leave `// indirect` dependencies unmanaged. The `otel-go` and `connectrpc-go` groups were dropped: measuring the declared constraints showed both families bind by lower bound only, so they are not version-coupled and upstream's separation of them stands (design D3).
- [x] 6.1a Verify family coupling from the declared constraints before writing any local group, rather than inferring it from version-number distance. Done for OpenTelemetry Go and Connect-RPC Go (lower bounds only → not coupled, rules removed) and for Aurelia (exact inter-package pins → coupled, rule kept). Note that Renovate's monorepo groups match by `matchSourceUrls`, not package name, so `group:opentelemetry-go` DOES apply to `go.opentelemetry.io/*` — an earlier draft of this task said otherwise.
- [x] 6.2 `backend/renovate.json`: disable `buf.build/gen/go/**` via `enabled: false` (not `ignoreDeps`, per design D7) so they stay visible on the dashboard. NOTE: `pocketsign/apis` was initially left enabled by design, but the first Renovate run showed the BSR version format defeats the `gomod` lookup entirely (`Could not determine new digest`), so it proposes nothing. It is excluded for INABILITY, not policy — re-enable it if Renovate gains BSR support.
- [x] 6.3 `frontend/renovate.json`: extend the org preset; add ONLY the `aurelia` group. Storybook, Vitest, OpenTelemetry JS, stylelint and Pulumi are grouped by maintained upstream presets — do not reimplement them (design D3). Leave `vite`, `workbox` and `connectrpc-js` to upstream coverage and add a local rule only if the observation phase shows them ungrouped.
- [x] 6.4 `frontend/renovate.json`: disable `@buf/liverty-music_schema.*` via `enabled: false`; exclude `@playwright/test` and all `overrides` entries from automerge.
- [x] 6.5 `cloud-provisioning/renovate.json`: extend the org preset. `group:pulumi` handles the provider grouping; add the automerge exclusion unconditionally. It is not pending anything — per design D6 the preview never runs on these pull requests, so no gate signal exists to make one conditional on, and 10.6 makes the exclusion permanent.
- [x] 6.6 Place `@biomejs/biome` on the same schedule in `frontend` and `cloud-provisioning` so the two exact pins are proposed together (design D3 — cross-repo grouping is not possible).
- [x] 6.7 Decide whether `specification` needs a repository-local `renovate.json` at all, or whether the inherited preset covers its GitHub Actions and mise axes (design Open Question 2). Create it only if needed.

## 7. Version fan-outs — derive from one source

- [x] 7.1 `backend`: replace the five `go-version: "1.27.0"` inputs across `test.yml`, `lint.yml` and `stripe-sandbox-e2e.yml` with `go-version-file: go.mod`. `setup-go` reads `go.mod` and, since v6, prefers its `toolchain` directive over `go`. This also removes the hazard the custom-manager approach existed to mitigate: `setup-go` and the build now resolve their version from the same declaration, so `GOTOOLCHAIN=auto` can no longer make CI build with a toolchain nobody proposed.
- [x] 7.1a `backend`: delete the `go: '1.27'` line from `.golangci.yml`. Documented default is "use Go version from the go.mod file", so the setting is a copy with no purpose.
- [x] 7.2 Verify on CI that `go build`, `go test` and `golangci-lint` all run on the version `go.mod` declares after 7.1/7.1a. This replaces the drift assertion the previous plan needed — there is no longer anything to drift.
- [x] 7.3 `frontend` and `cloud-provisioning`: add a `.nvmrc` containing the major in use (`22`) and replace all nine `node-version: '22'` inputs with `node-version-file: .nvmrc`.
- [x] 7.3a Leave the three `node:22-alpine` tags, `cloud-provisioning`'s `engines.node` range and the two `@types/node` entries alone. Each records the version at its own precision and is already handled by a native Renovate manager (`dockerfile`, `npm`); they are dependencies, not copies to be synchronised.
- [x] 7.4 Add a `customManagers` entry binding `@playwright/test` to the `mcr.microsoft.com/playwright` container tag in `ci.yaml` AND to the same tag in the baseline-regeneration command documented in `frontend/AGENTS.md`. This is the ONLY remaining fan-out: its third location is prose in a contributor document, which no tool can derive from. All three move together, grouped and automerge-disabled.

## 8. GitHub Actions SHA pinning

- [x] 8.1 Let `helpers:pinGitHubActionDigests` raise the pinning pull requests. RESULT: it did, and nothing was hand-edited. It also over-reached — see 8.3.
- [x] 8.2 Review and merge the pinning pull requests per repository. RESULT: merged for `.github`, `cloud-provisioning` and `specification`. As a first exercise of the pipeline it found two things a merge-and-move-on would have missed: the self-pinning in 8.3, and that `claude-code-action` refuses bot-authored pull requests by default, so EVERY Renovate PR carried a permanently red `review` check. That check is not in `CI Success`'s `needs` so it never blocked anything — which is exactly why it was worth fixing: a check that is red on every dependency PR trains people to read red as normal. Fixed by naming `renovate` in `allowed_bots` (explicit list, never `'*'` — these repositories are public, and an unrestricted list would let any third-party App invoke the action with a prompt it controls).
- [x] 8.3 Confirm liverty-music-owned references are left on `@main`. RESULT: they were NOT, initially — the preset pinned `liverty-music/.github/.github/workflows/claude-review.yml@main` in all three repositories, which would have severed the one property a reusable workflow exists for. A `pinDigests: false` rule for `liverty-music/**` was added to the org preset and the pull requests regenerated without it. Verified on `main`: every third-party `uses:` is a SHA, and the liverty-music reference is still `@main`.
- [ ] 8.4 Verify `imjasonh/setup-crane` ends up with a single consistent reference: it currently appears both SHA-pinned (`@31b88efe…# v0.4`) and tag-pinned (`@v0.4`) in different workflows.
- [ ] 8.5 Confirm the pinning resolves the deprecation warning surfaced on this change's own PR (`actions/checkout@v4` and `dorny/paths-filter@v3` targeting the deprecated Node 20 runtime) by advancing them, not merely by freezing them at a stale commit.

## 9. Observation phase

- [x] 9.1 Let one full weekly cycle run with automerge still globally off. RESULT: the cycle was run on demand rather than waiting for Saturday (schedule lifted, then restored). Ten pull requests across five repositories, with the rest held by `prConcurrentLimit: 10` — roughly 29 rate-limited in cloud-provisioning alone. The ~36/month estimate in D10 looks the right order of magnitude once the initial backlog clears, but a genuine steady-state figure needs a real week and is not claimed here.
- [x] 9.2 Verify each fan-out actually matched. RESULT: Go needed no manager — `toolchain` was proposed alone (`v1.27.1`) while the `go` directive and the Dockerfile tag correctly stayed put, which is D12's projection rule observed rather than argued. Node is handled by the upstream group, which proposes `node` and `@types/node` together. The Playwright custom manager — the only one left — matched all three of its locations and produced a `playwright` group.
- [x] 9.5 Confirm Renovate keeps `aurelia` on `2.0.0-rc.2`. RESULT: confirmed. The dashboard shows `@aurelia/testing ^2.0.0-rc.2 → [Updates: 2.0.0-rc.2]` — no `2.1.0-dev.*` proposal anywhere. D16 was right that the default handles this, and NO rule was written.
- [x] 9.3 Confirm the disabled schema SDKs appear on the dependency dashboard. RESULT: confirmed — and the same mechanism now carries three more exclusions found during the cycle (our own container images, the pocketsign BSR modules, our own reusable workflow). `enabled: false` over `ignoreDeps` earned itself: every one of them stays visible rather than silently vanishing.
- [x] 9.4 Adjust grouping based on observed volume before enabling any automerge. RESULT: one adjustment needed — `group:pulumi` does not group the Pulumi providers, so a local group was added (as noise reduction; measuring showed they are not version-coupled). Everything else the D3 table claimed was verified: Vitest, OpenTelemetry JS and the Node family are grouped by upstream as assumed.

## 10. Enable automerge

- [ ] 10.1 Add `allowAutoMerge: true` to `defaultRepositoryArgs` in `cloud-provisioning/src/github/components/organization.ts`. Note this reaches all five repositories including `.github`; the gate against unattended merges there is the Renovate-side exclusion from 5.1d, since `.github` has no branch protection to rely on. Confirm `requiredStatusCheckContexts` stays `['CI Success']` on the other four and that the enabled merge strategy remains merge-commit-only (`allowSquashMerge` and `allowRebaseMerge` are both false).
- [ ] 10.2 Run `pulumi up -s prod` and verify the setting is live on all participating repositories.
- [ ] 10.3 Enable automerge for the lowest-risk groups first: `devDependencies`, GitHub Actions, Docker digests, mise. Observe one cycle.
- [ ] 10.4 Enable automerge for the remaining families: the two local groups that survived the coupling check (`go-tools`, `aurelia`) and the upstream-grouped Vitest, Storybook, OpenTelemetry JS, stylelint, Vite and workbox families, plus `typescript`, `biome`, the `pannpers/*` libraries, `stripe-go`, `zitadel/*`, the `pocketsign` schema SDK, and the OpenTelemetry Go and Connect-RPC Go modules — the last two now arriving as upstream's own `opentelemetry-go` / `opentelemetry-go-contrib` groups and individually, since the local `otel-go` / `connectrpc-go` groups were removed (design D3).
- [ ] 10.4b Gate the `aurelia` group on task 2.8 having repaired the app-shell tests (design D17). Do not fall back to human review if 2.8 slips — a reviewer reading an Aurelia version bump sees no more than the pipeline does.
- [ ] 10.4a `workbox` may automerge: task 2.5 closed the precache gap. The `pwa` project now runs from `playwright.pwa.config.mjs` against a production build and asserts the service worker activates, controls the page, and precaches the app shell plus its route chunks — which is what a workbox upgrade breaks. Confirm that job is green and required before enabling.
- [ ] 10.4c Do NOT automerge `vite-plugin-pwa`. Task 2.5 closed the caching half of its surface but not the manifest / install-prompt half: `pwa-install-prompt.spec.ts` cannot run in CI (`beforeinstallprompt` needs browser install heuristics) and is an enumerated gap in `frontend/docs/ci-coverage-gaps.md` with no control. Do NOT route it through human review instead — a reviewer sees only a version and lock file and cannot evaluate install-prompt behaviour. Leave it proposed-but-unmerged until follow-up 12.3 names a control (spec: "Coverage is missing and no reviewer could evaluate the risk").
- [ ] 10.5 Enable automerge for the Go-version and Node-version groups, patch-level only at first. Expect such a PR to leave `golang:1.27-alpine`, `go: '1.27'`, `node:22-alpine` and `engines.node` untouched — those record coarser precision and are already satisfied (design D12). Verify this reads as complete rather than partial in the custom manager's output.
- [ ] 10.6 Leave the `pulumi` group excluded from automerge permanently (design D6). A reviewer merges it after running `pulumi preview` themselves, as the PR template and runbooks already prescribe. Do not reintroduce a preview-gated automerge: it is the exposure D6 removes.
- [ ] 10.7 Verify the rollback path: setting automerge off in the org preset stops unattended merges across all four repositories in one commit, with PR creation still running.

## 11. Documentation

- [x] 11.1 Reword the `toolchain` comment in `backend/go.mod` as a floor-and-history record (design D8): keep the nine `GO-2026-*` advisories as the reason the floor was set, and note that the directive is advanced automatically. Its claim must stay true after a bump.
- [x] 11.2 Document the `overrides` exit conditions in `frontend` — `fflate` and `dompurify` are removed, not upgraded, once `posthog-js` widens its ranges (design D9) — so a future reader does not mistake the exact `fflate` pin for neglect.
- [x] 11.3 Add a short runbook covering the recurring human actions: regenerating Storybook visual baselines after a `@playwright/test` bump, reviewing a Pulumi preview that reports changes, handling an update held back by the release-age delay when it is genuinely urgent (the delay is NOT bypassed for vulnerability fixes — Renovate skips `schedule` and the concurrency limits for those, but not `minimumReleaseAge`), and running `buf dep update` in `specification`. That last one is the only fully manual axis left: Renovate has no Buf Schema Registry manager, and `buf.lock` pins third-party modules (`bufbuild/protovalidate`, `googleapis/googleapis`) where falling behind is the risk, so nothing will remind anyone but this runbook.

## 12. Follow-ups (not required for this change)

- [x] 12.1 ~~Replace the Go-version custom manager with single-source derivation.~~ DONE IN THIS CHANGE, not deferred — see section 7 and design D4. The custom manager was never written: `setup-go`'s `go-version-file` and golangci-lint's go.mod default made the duplication removable outright, which is strictly better than grouping it. Kept here rather than deleted so the reversal is visible.
- [ ] 12.3 Find a way to cover `pwa-install-prompt.spec.ts`, the one PWA gap that genuinely cannot be closed in headless CI. Until then `vite-plugin-pwa` manifest behavior has no pipeline coverage (design Risk 2).
- [ ] 12.4 Decide whether `.github` should get its own CI and branch protection so it can participate in automerge like the other four (design D11).
