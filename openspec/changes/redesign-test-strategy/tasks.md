## 1. Infrastructure Setup

- [x] 1.1 Add `toHaveScreenshot` config to `playwright.config.mjs` (maxDiffPixelRatio, animations disabled, snapshotPathTemplate)
- [x] 1.2 Add `e2e/__screenshots__/` to `.gitignore`
- [x] 1.3 Create `e2e/functional/`, `e2e/visual/`, `e2e/pwa/` directories
- [x] 1.4 Update Playwright config projects: rename `mobile-layout` → `mobile-visual`, add `functional` and `pwa` projects with correct testMatch patterns

## 2. Directory Restructuring (git mv)

- [x] 2.1 `git mv e2e/onboarding-flow.spec.ts e2e/functional/`
- [x] 2.2 `git mv e2e/detail-sheet-dismiss.spec.ts e2e/functional/`
- [x] 2.3 `git mv e2e/css-antipattern-verification.spec.ts e2e/functional/`
- [x] 2.4 `git mv e2e/dashboard-lane-classification.spec.ts e2e/functional/`
- [x] 2.5 `git mv e2e/snack-bar.spec.ts e2e/functional/`
- [x] 2.6 `git mv e2e/artist-image-ui.spec.ts e2e/functional/`
- [x] 2.7 `git mv e2e/pwa-install-prompt.spec.ts e2e/pwa/`
- [x] 2.8 `git mv e2e/pwa-offline-cache.spec.ts e2e/pwa/`
- [x] 2.9 `git mv e2e/pwa-circuit-precache.spec.ts e2e/pwa/`
- [x] 2.10 `git mv e2e/pwa-settings.spec.ts e2e/pwa/`
- [x] 2.11 `git mv e2e/layout/shell.layout.spec.ts e2e/visual/shell.visual.spec.ts`
- [x] 2.12 `git mv e2e/layout/dashboard.layout.spec.ts e2e/visual/dashboard.visual.spec.ts`
- [x] 2.13 `git mv e2e/layout/discover.layout.spec.ts e2e/visual/discover.visual.spec.ts`
- [x] 2.14 `git mv e2e/layout/my-artists.layout.spec.ts e2e/visual/my-artists.visual.spec.ts`
- [x] 2.15 `git mv e2e/layout/ticket-journey.layout.spec.ts e2e/visual/ticket-journey.visual.spec.ts`
- [x] 2.16 `git mv e2e/layout/settings.auth.spec.ts e2e/visual/settings.visual.spec.ts`
- [x] 2.17 Delete `e2e/layout/assertions.ts` and `e2e/layout/fixtures.ts` after migration

## 3. Visual Regression Tests (Layer 4 — rewrite layout tests)

- [x] 3.1 Rewrite `e2e/visual/shell.visual.spec.ts` — replace 4 boundingBox tests with toHaveScreenshot
- [x] 3.2 Rewrite `e2e/visual/dashboard.visual.spec.ts` — replace 21 boundingBox tests with toHaveScreenshot (dashboard loaded, empty state, after scroll, blurred state)
- [x] 3.3 Rewrite `e2e/visual/discover.visual.spec.ts` — replace 12 boundingBox tests with toHaveScreenshot (bubble mode, search mode)
- [x] 3.4 Rewrite `e2e/visual/my-artists.visual.spec.ts` — replace 8 boundingBox tests with toHaveScreenshot (with artists, empty state)
- [x] 3.5 Rewrite `e2e/visual/ticket-journey.visual.spec.ts` — replace 5 boundingBox tests with toHaveScreenshot (badge visible, detail sheet open)
- [x] 3.6 Rewrite `e2e/visual/settings.visual.spec.ts` — replace 3 boundingBox tests with toHaveScreenshot
- [x] 3.7 Initial baselines generated by CI Visual Regression job with `--update-snapshots`

## 4. Component Integration Tests (Layer 2 — new tests)

- [x] 4.1 Create `src/components/bottom-sheet/bottom-sheet.spec.ts` — open/close state, slotted content, sheet-closed event
- [x] 4.2 Create `src/components/coach-mark/coach-mark.spec.ts` — visible state, onTap callback, deactivate
- [x] 4.3 Create `src/components/celebration-overlay/celebration-overlay.spec.ts` — data-state attribute, tap-to-dismiss
- [x] 4.4 Create `src/components/snack-bar/snack-bar.spec.ts` — toast rendering in snack-stack, toast count
- [x] 4.5 Create `src/components/live-highway/concert-highway.spec.ts` — beam index, dateGroups, detaching cleanup
- [x] 4.6 Create `src/components/bottom-nav-bar/bottom-nav-bar.spec.ts` — nav items render, active state

## 5. Migrate DOM/Text Tests from E2E to Layer 2

- [x] 5.1 Extract DOM structure tests from `e2e/visual/dashboard.visual.spec.ts` (header labels, empty state text, discover link) into `concert-highway.spec.ts` — covered by Group 3 rewrite (DOM tests removed) + Group 4 L2 tests
- [x] 5.2 Extract DOM structure tests from `e2e/visual/my-artists.visual.spec.ts` (header title, artist count, row count, row names, empty state, header hidden) — covered by Group 3 rewrite (DOM tests removed)
- [x] 5.3 Extract DOM structure tests from `e2e/visual/ticket-journey.visual.spec.ts` (badge data-attr, journey section, heading text, button count, button attrs) — covered by Group 3 rewrite (DOM tests removed)
- [x] 5.4 Visual State tests in onboarding-flow (getComputedStyle) kept in E2E — require real browser for CSS property assertions
- [x] 5.5 Visual State tests in css-antipattern-verification (data-state, fade-out) kept in E2E — require real browser
- [x] 5.6 Remove migrated tests from E2E files — visual tests already rewritten to toHaveScreenshot only

## 6. Replace waitForTimeout in E2E Tests

- [x] 6.1 Dashboard scroll waitForTimeout — removed during Group 3 visual rewrite (no longer in codebase)
- [x] 6.2 Replace `waitForTimeout(2000/500/5000)` in onboarding-flow with `waitForLoadState('networkidle')` and `toHaveCount(0, { timeout })`
- [x] 6.3 Replace `waitForTimeout(100/3000)` in snack-bar with `toHaveCount(0, { timeout: 5000 })`
- [x] 6.4 Replace `waitForTimeout(2000)` in no-console-errors with `waitForLoadState('networkidle')`

## 7. CI Pipeline Update

- [x] 7.1 Update `.github/workflows/ci.yaml` E2E job to run `--project=functional --project=pwa`
- [x] 7.2 Smoke job already runs `--project=smoke --project=onboarding` — no change needed
- [x] 7.3 Add Visual Regression CI job with baseline download, test run, and artifact upload
- [x] 7.4 Visual job uses `--update-snapshots` and uploads baselines as artifact (overwrite: true)

## 8. Cleanup and Verification

- [x] 8.1 Verify zero `boundingBox()` in `e2e/pwa/` (0 calls). 2 remaining in `e2e/functional/` are functional checks (swipe dismiss, toast z-order), not coordinate assertions.
- [x] 8.2 Verify zero `waitForTimeout()` calls in `e2e/functional/` and `e2e/visual/` — confirmed 0 occurrences
- [x] 8.3 Verify all tests pass locally: `make test` — 92 files, 954 passed, 0 failed
- [ ] 8.4 Verify CI pipeline: all jobs green — requires PR push
- [x] 8.5 Delete empty `e2e/layout/` directory — already removed during Group 2
